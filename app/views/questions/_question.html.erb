<li><%= link_to question.title, question_path(question) %>
  <h4><%= question.total_votes %>
  </li>
  <%= button_to 'Upvote', upvote_question_path(question.id), remote: true %><%= button_to 'Downvote', downvote_question_path(question.id), remote: true %>